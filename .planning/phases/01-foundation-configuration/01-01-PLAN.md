---
phase: 01-foundation-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - src/cer_scraper/__init__.py
  - src/cer_scraper/config/__init__.py
  - src/cer_scraper/db/__init__.py
  - src/cer_scraper/logging/__init__.py
  - tests/__init__.py
autonomous: true

must_haves:
  truths:
    - "uv run python -c 'import cer_scraper' succeeds without error"
    - "sqlalchemy, pydantic-settings, and python-json-logger are installed and importable"
    - ".gitignore excludes .env, data/, logs/, __pycache__/, .venv/"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata and dependency declarations"
      contains: "sqlalchemy"
    - path: "src/cer_scraper/__init__.py"
      provides: "Root package marker"
    - path: ".gitignore"
      provides: "Version control exclusions for secrets and runtime data"
      contains: ".env"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv add installs deps and updates lockfile"
      pattern: "sqlalchemy|pydantic-settings|python-json-logger"
---

<objective>
Create the project package structure and install all Phase 1 dependencies so that subsequent plans can immediately write module code without setup concerns.

Purpose: Every other plan in this phase needs the package structure and libraries in place. This plan removes that bottleneck as a standalone Wave 1 task.
Output: A working Python package at `src/cer_scraper/` with all sub-packages and Phase 1 dependencies installed.
</objective>

<execution_context>
@C:\Users\amelv\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\amelv\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-configuration/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package directory structure</name>
  <files>
    src/cer_scraper/__init__.py
    src/cer_scraper/config/__init__.py
    src/cer_scraper/db/__init__.py
    src/cer_scraper/logging/__init__.py
    tests/__init__.py
  </files>
  <action>
Create the following directory tree with empty `__init__.py` files:

```
src/
  cer_scraper/
    __init__.py          # Contains __version__ = "0.1.0"
    config/
      __init__.py        # Empty
    db/
      __init__.py        # Empty
    logging/
      __init__.py        # Empty
tests/
  __init__.py            # Empty
```

The `src/cer_scraper/__init__.py` should contain only `__version__ = "0.1.0"`.
All other `__init__.py` files should be empty (no imports, no code).

Also create empty directories that will be populated at runtime:
- `config/` (for YAML config files -- created by Plan 02)
- `data/` (for SQLite DB -- created at runtime)
- `logs/` (for log files -- created at runtime)

Note: `data/` and `logs/` should NOT have files committed. They are gitignored.
  </action>
  <verify>
Run: `uv run python -c "from pathlib import Path; assert Path('src/cer_scraper/__init__.py').exists(); assert Path('src/cer_scraper/config/__init__.py').exists(); assert Path('src/cer_scraper/db/__init__.py').exists(); assert Path('src/cer_scraper/logging/__init__.py').exists(); assert Path('tests/__init__.py').exists(); print('All package directories exist')"`
  </verify>
  <done>All five __init__.py files exist. The src/cer_scraper/ package has config/, db/, and logging/ sub-packages.</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and configure project</name>
  <files>
    pyproject.toml
    .gitignore
  </files>
  <action>
1. Update `pyproject.toml` to include the `src` layout configuration. Add under [project]:

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cer_scraper"]
```

2. Install Phase 1 dependencies using uv:

```bash
uv add sqlalchemy "pydantic-settings[yaml]" python-json-logger
```

This will update pyproject.toml dependencies and uv.lock automatically. The `[yaml]` extra for pydantic-settings pulls in PyYAML as a transitive dependency.

3. Create `.gitignore` with these entries:

```gitignore
# Secrets
.env

# Runtime data
data/
logs/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
```

4. Verify the package is importable:

```bash
uv run python -c "import cer_scraper; print(cer_scraper.__version__)"
```

This should print "0.1.0". If it fails with an import error, the src layout configuration in pyproject.toml needs adjustment.

IMPORTANT: Do NOT add `data/` or `logs/` directories to git. They are gitignored and created at runtime by the application code.
  </action>
  <verify>
Run all three:
1. `uv run python -c "import sqlalchemy; print(f'SQLAlchemy {sqlalchemy.__version__}')"` -- prints version >= 2.0
2. `uv run python -c "import pydantic_settings; print('pydantic-settings OK')"` -- prints OK
3. `uv run python -c "import pythonjsonlogger; print('python-json-logger OK')"` -- prints OK
4. `uv run python -c "import cer_scraper; print(cer_scraper.__version__)"` -- prints "0.1.0"
  </verify>
  <done>pyproject.toml has all 3 dependencies listed, uv.lock is updated, .gitignore exists with correct entries, and `import cer_scraper` works from the src layout.</done>
</task>

</tasks>

<verification>
1. `uv run python -c "import cer_scraper; import sqlalchemy; import pydantic_settings; import pythonjsonlogger; print('All imports OK')"` succeeds
2. `.gitignore` contains entries for `.env`, `data/`, `logs/`, `__pycache__/`, `.venv/`
3. Directory structure matches the research architecture pattern
</verification>

<success_criteria>
- The cer_scraper package is importable via `uv run python -c "import cer_scraper"`
- All three core dependencies (sqlalchemy, pydantic-settings, python-json-logger) are installed and importable
- .gitignore prevents secrets and runtime data from being committed
- The src layout is properly configured in pyproject.toml
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-configuration/01-01-SUMMARY.md`
</output>
